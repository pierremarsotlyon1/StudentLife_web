/**
 * Created by pierremarsot on 24/07/2017.
 */
const initialState = {
  token: '',
};

export default function auth(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}